// 1
class Node {
    constructor(data, left, right) {
        this.data = data;
        this.left = left;
        this.right = right;
    }
}

// 2
class Tree {
    constructor(array, root) {
        this.array = array;
        this.root = root;
    }
}

// 3
function buildTree(array) {
    
}